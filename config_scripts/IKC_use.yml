# Configuration file description:
# Interpolation method comes from Step2 of:
# Two-Stage Self-supervised Cycle-Consistency Network for Reconstruction of Thin-Slice MR Images


device: 'cuda:0'                 # 'cpu' when cuda not available, else 'cuda:0'
model_config:
  model_name: 'unet2d_gan'          # Name of the model
  checkpoint_path: '/home/yxsun/win_file/semi-3/Code/TrainFramework_torch/results/checkpoints/checkpoint-20220110-122809.pth'         # Path to load from saved checkpoints. Enter 'null' when train from scratch
train_config:
  strategy_name: ''   # Strategy for training


